# Pass Management System - Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Passes List  │  │ Pass Detail  │  │ Admin Panel  │          │
│  │   /passes    │  │/passes/[id]  │  │/admin/passes │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                 │                   │                  │
│         └─────────────────┴───────────────────┘                  │
│                           │                                       │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API LAYER                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ User APIs                                                   │ │
│  │  • GET  /api/passes (list with filters)                   │ │
│  │  • GET  /api/passes/[id] (pass details)                   │ │
│  │  • POST /api/passes/[id]/join (request to join)           │ │
│  │  • GET  /api/passes/[id]/join (membership status)         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Admin APIs                                                  │ │
│  │  • GET  /api/admin/passes (list admin's passes)           │ │
│  │  • POST /api/admin/passes (create pass)                   │ │
│  │  • GET  /api/admin/passes/memberships/approve (requests)  │ │
│  │  • POST /api/admin/passes/memberships/approve (action)    │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BUSINESS LOGIC                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │   Location     │  │  Membership    │  │    Wallet        │  │
│  │   Filtering    │  │   Management   │  │  Integration     │  │
│  │                │  │                │  │                  │  │
│  │ • Haversine    │  │ • Join Request │  │ • Apple Wallet   │  │
│  │ • 20km Radius  │  │ • Approval     │  │ • Google Pay     │  │
│  │ • Distance     │  │ • Status Track │  │ • Pass Gen       │  │
│  └────────────────┘  └────────────────┘  └──────────────────┘  │
│                                                                   │
└───────────────────────────┼───────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ admin_passes                                                │ │
│  │  • _id, name, description, type, category                  │ │
│  │  • location { lat, lng, name }                             │ │
│  │  • dateStart, dateEnd, status                              │ │
│  │  • createdByAdminId, createdAt, updatedAt                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ user_pass_memberships (NEW)                                │ │
│  │  • _id, userId, userEmail, passId                          │ │
│  │  • status (pending/approved/rejected)                      │ │
│  │  • requestedAt, approvedAt, rejectedAt                     │ │
│  │  • approvedBy, rejectedBy                                  │ │
│  │  • addedToWallet, walletAddedAt                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ users                                                       │ │
│  │  • _id, email, name, role                                  │ │
│  │  • createdByAdminId (for employees)                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## User Flow Diagram

```
┌─────────────┐
│   START     │
│  User Login │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│  Browse Passes      │
│  • Select Category  │
│  • Location Filter  │
│  • Search           │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  View Pass Details  │
│  • Name, Desc       │
│  • Location, Date   │
│  • Type, Category   │
└──────┬──────────────┘
       │
       ▼
    ┌──┴──┐
    │ Has │
    │Mem? │
    └──┬──┘
       │
   ┌───┴───┐
   │       │
  No      Yes
   │       │
   ▼       ▼
┌─────┐ ┌──────────────┐
│Join │ │Check Status  │
│Pass │ │              │
└──┬──┘ └──┬───────────┘
   │       │
   │    ┌──┴──┐
   │    │     │
   │  Pending Approved
   │    │     │
   ▼    ▼     ▼
┌──────────┐ ┌────────────┐
│ Pending  │ │Add to      │
│ Approval │ │Wallet      │
│          │ │            │
│ Wait for │ │• Apple     │
│  Admin   │ │• Google    │
└──────────┘ └────────────┘
```

## Admin Flow Diagram

```
┌─────────────┐
│   START     │
│ Admin Login │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│  Admin Dashboard        │
│  /admin/passes/         │
│  memberships            │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  View Requests          │
│  • Pending (Amber)      │
│  • Approved (Green)     │
│  • Rejected (Red)       │
│  • All                  │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Review Request         │
│  • User Info            │
│  • Pass Name            │
│  • Request Date         │
└──────┬──────────────────┘
       │
       ▼
    ┌──┴──┐
    │Take │
    │Action│
    └──┬──┘
       │
   ┌───┴───┐
   │       │
Approve  Reject
   │       │
   ▼       ▼
┌─────┐ ┌─────┐
│Set  │ │Set  │
│to   │ │to   │
│Appr │ │Rej  │
│oved │ │ected│
└──┬──┘ └──┬──┘
   │       │
   └───┬───┘
       │
       ▼
┌─────────────┐
│Update DB    │
│Notify User  │
└─────────────┘
```

## Category Structure

```
Pass Categories
├── All Passes (default)
├── Concerts 🎵
├── Workplace 💼
├── Events 🎉
├── Retail 🛍️
├── Access 🏢
├── Community 👥 (NEW)
├── Temples ⛪ (NEW)
└── Spiritual 💗 (NEW)
```

## Location-Based Filtering

```
User Location (lat, lng)
        │
        ▼
┌───────────────────┐
│ Get All Passes    │
│ with Location     │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│ For Each Pass:    │
│ Calculate Distance│
│ (Haversine)       │
└────────┬──────────┘
         │
         ▼
    ┌────┴────┐
    │Distance │
    │<= 20km? │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
   Yes       No
    │         │
    ▼         ▼
 Include   Exclude
    │
    ▼
┌────────────────┐
│ Return Filtered│
│ Passes         │
└────────────────┘
```

## Membership Status States

```
┌─────────────┐
│  No Member  │ ──────┐
└─────────────┘       │
                      │ User Clicks
                      │ "Join Pass"
                      │
                      ▼
              ┌───────────────┐
              │   PENDING     │
              │   (Amber)     │
              └───────┬───────┘
                      │
          ┌───────────┴───────────┐
          │                       │
    Admin Approves          Admin Rejects
          │                       │
          ▼                       ▼
  ┌───────────────┐       ┌───────────────┐
  │   APPROVED    │       │   REJECTED    │
  │   (Green)     │       │   (Red)       │
  │               │       │               │
  │ Can Add to    │       │ Contact Admin │
  │ Wallet        │       │               │
  └───────────────┘       └───────────────┘
```

## Data Flow: Join Request

```
1. User Action
   ├─ Click "Request to Join"
   └─ POST /api/passes/[id]/join

2. API Processing
   ├─ Verify user is logged in
   ├─ Check pass exists and is active
   ├─ Check no existing membership
   └─ Create membership record

3. Database
   ├─ Insert into user_pass_memberships
   │  ├─ userId: ObjectId
   │  ├─ passId: ObjectId
   │  ├─ status: "pending"
   │  └─ requestedAt: Date
   └─ Return membership ID

4. UI Update
   ├─ Show "Pending Approval" status
   ├─ Display amber badge
   └─ Hide wallet buttons
```

## Data Flow: Admin Approval

```
1. Admin Action
   ├─ View pending requests
   ├─ Click "Approve" or "Reject"
   └─ POST /api/admin/passes/memberships/approve

2. API Processing
   ├─ Verify admin is logged in
   ├─ Check membership exists
   ├─ Verify status is "pending"
   └─ Update membership record

3. Database
   ├─ Update user_pass_memberships
   │  ├─ status: "approved" or "rejected"
   │  ├─ approvedAt/rejectedAt: Date
   │  └─ approvedBy/rejectedBy: AdminId
   └─ Return updated membership

4. UI Update
   ├─ Refresh membership list
   ├─ Show success message
   └─ Update badge color

5. User Notification (Future)
   ├─ Email notification
   ├─ In-app notification
   └─ Push notification
```

## Security Considerations

```
┌─────────────────────────────────────┐
│ Authentication & Authorization      │
├─────────────────────────────────────┤
│ • Session-based auth (NextAuth)     │
│ • Admin cookie verification         │
│ • User ownership validation         │
│ • Pass status verification          │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Data Validation                     │
├─────────────────────────────────────┤
│ • ObjectId validation               │
│ • Status enum validation            │
│ • Duplicate request prevention      │
│ • Input sanitization                │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Rate Limiting (Future)              │
├─────────────────────────────────────┤
│ • Join request limits               │
│ • API call throttling               │
│ • Approval action limits            │
└─────────────────────────────────────┘
```

## Performance Optimizations

```
┌─────────────────────────────────────┐
│ Database Indexes                    │
├─────────────────────────────────────┤
│ • admin_passes                      │
│   ├─ { status: 1, category: 1 }    │
│   ├─ { location.lat: 1, lng: 1 }   │
│   └─ { createdByAdminId: 1 }       │
│                                     │
│ • user_pass_memberships             │
│   ├─ { userId: 1, passId: 1 }      │
│   ├─ { passId: 1, status: 1 }      │
│   └─ { status: 1 }                 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Caching Strategy (Future)           │
├─────────────────────────────────────┤
│ • Pass list caching (5 min)         │
│ • Location-based cache              │
│ • User membership cache             │
│ • Invalidate on updates             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Query Optimization                  │
├─────────────────────────────────────┤
│ • Aggregation pipelines             │
│ • Projection (select fields)        │
│ • Limit results                     │
│ • Pagination (future)               │
└─────────────────────────────────────┘
```

This architecture provides a scalable, secure, and performant system for managing pass memberships with location-based filtering and approval workflows.
